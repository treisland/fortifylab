program: fortifylab-python-tui-migration
branch: migration/python-tui
base_branch: dev
current_milestone: M9
overall_status: active

goal:
  primary_objective: >-
    Replace FortifyLab's current Bash wizard with a maintainable Python CLI/TUI
    application while preserving the existing navigation structure and lab
    operation behavior.
  preserve:
    - CLI-first application direction
    - Existing wizard navigation structure
    - Safe lab deployment and lifecycle behavior
    - Proven Bash deployment scripts as temporary operation adapters
  improve:
    - Maintainability for future repo owners
    - Testability of navigation, config, operations, and status flows
    - Arrow-key selection and jump-to-number menu navigation
    - Explicit module boundaries and typed application state
    - Config safety, diagnostics, logging, and contributor onboarding
  out_of_scope:
    - Building a web UI
    - Preserving deprecated Python preview code as-is
    - Rewriting every Helm or Kubernetes command in the first milestones
    - Changing FortifyLab's lab/training purpose

heartbeat:
  cadence: daily
  stale_after_days: 3
  last_updated: 2026-08-27
  milestone: M9.7
  workstream: pm-M9.7-closeout-open-next
  branch: agent/pm-M9.7-closeout-open-next
  status: closeout
  changed:
    - Closed M9.6 Setup and Readiness workflow on migration/python-tui.
    - Functional workflows now cover Help/Runbooks, Configuration, Diagnostics/Status, Lifecycle Operations, Logs/Wizard Log, and Setup/Readiness.
    - User chose to keep PR #479 open and continue adding focused parity work before dev merge.
    - Opened and completed M9.7 Guided Deployment workflow as the next focused slice.
  next:
    - Await user approval before opening M9.8 Dashboard Access, URLs, And Credentials workflow.
    - Keep PR #479 held out of dev until the user explicitly approves merge-back.
  blockers: []
  risks:
    - Guided deployment can sprawl; keep M9.7 scoped to the guided deployment workflow only.
    - Default tests must not require Kubernetes, Helm, Docker, network, credentials, or live lab state.
    - Mutating operations must require preview plus explicit confirmation and should reuse the operation catalog/adapters.

  needs_decision:
    - None; M9.7 sequencing is approved by the user's proceed instruction.


open_decisions:
  - id: tui-framework
    status: accepted
    recommendation: Textual
    decision_needed_by: M1
    notes: >-
      Textual is accepted for the long-term TUI because it supports richer
      screens, keybindings, status panes, logs, and future interactive workflows.
  - id: branch-upstream
    status: accepted
    recommendation: Push migration/python-tui and set upstream to origin/migration/python-tui.
    decision_needed_by: M0
    notes: >-
      The local branch was created from origin/dev and initially tracks origin/dev.
      After first push, the upstream should be the migration integration branch.

branch_rules:
  integration_branch: migration/python-tui
  base_branch: dev
  focused_branch_pattern: agent/<workstream>-<milestone>-<short-topic>
  pr_target: migration/python-tui
  merge_back_to_dev_after: M8
  rules:
    - All migration implementation PRs target migration/python-tui.
    - Keep workstreams narrow and file ownership explicit.
    - Every PR updates this tracker when status, scope, risks, or blockers change.
    - Every milestone requires acceptance criteria and relevant tests before advancing.
    - Do not redesign navigation during M1/M2; preserve current structure first.

workstreams:
  pm:
    owner: pm-agent
    status: active
    branch: agent/pm-M9.3-closeout-open-M9.4
    owns:
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Close M9 workflow parity and prepare PR #479 dev merge readiness.
    next_task: Merge migration/python-tui to dev through PR #479 after final checks.
    blockers: []
  architecture:
    owner: architecture-agent
    status: complete
    branch: agent/architecture-M1-skeleton
    owns:
      - fortifylab/
      - bin/fortifylab
      - start_wizard.sh
      - requirements-python.txt
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Implement target Python package layout and entrypoint transition for M1.
    result: M1 skeleton merged into migration/python-tui via PR #451.
    changed:
      - Root-level fortifylab package skeleton added.
      - ./bin/fortifylab now imports from the new package instead of src/.
      - start_wizard.sh converted to a compatibility shim.
      - TUI smoke-test contract coordinated with tests.
    blockers: []
    risks:
      - Legacy start_wizard.sh subcommands are not implemented in M1.
      - Deprecated src/ preview code remains until cleanup is handled by later milestones.
    last_heartbeat: 2026-08-25
  navigation:
    owner: navigation-agent
    status: complete
    branch: agent/navigation-M2-menu-model
    owns:
      - fortifylab/navigation/
      - tests/test_m2_navigation_model.py
      - docs/development/python-tui-migration.md
      - .migration/python-tui-plan.yml
    current_task: Completed deterministic Python menu model for the documented Bash wizard baseline.
    result: Navigation Model PR #454 merged into migration/python-tui.
    blockers: []
    last_heartbeat: 2026-08-25
  tui:
    owner: tui-agent
    status: complete
    branch: agent/tui-M2-keybindings
    owns:
      - fortifylab/tui/
      - fortifylab/navigation/
      - tests/
    current_task: Completed Textual shell and keybindings against the M2 menu model.
    result: TUI Keybindings PR #455 merged into migration/python-tui.
    changed:
      - Pure controller API exists in fortifylab.navigation.controller.
      - Arrow, jump-to-number, Enter activation, back, help, quit, disabled, and multi-digit key paths are covered.
      - TUI shell renders the shared navigation model and avoids real operation wiring in M2.
    blockers: []
    last_heartbeat: 2026-08-25
  operations:
    owner: operations-agent
    status: complete
    branch: agent/operations-M3-adapter-catalog
    owns:
      - fortifylab/operations/
      - tests/test_m3_operation_adapters.py
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Completed M3 Bash lifecycle operation catalog and safe runner.
    result: Operations Adapter PR #458 merged into migration/python-tui.
    changed:
      - Added typed operation, command plan, preview, command result, and operation result models.
      - Added catalog entries for core component, sample app, and all-lab lifecycle scripts.
      - Added dry-run previews, confirmation-required metadata, and a redacting execution wrapper.
      - Added harmless command tests that do not invoke Kubernetes, Helm, Docker, or real lifecycle scripts.
    blockers: []
    risks:
      - Selected-profile lifecycle execution still depends on future guided workflow state wiring.
      - M3 runner captures command output; future TUI work may stream incrementally instead.
    last_heartbeat: 2026-08-25
  config:
    owner: config-agent
    status: complete
    branch: agent/config-M4-cli-bridge
    owns:
      - fortifylab/config/
      - fortifylab/cli.py
      - start_wizard.sh
      - tests/test_m4_config_engine.py
      - tests/test_m4_config_cli_bridge.py
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Completed Python-native config engine and supported CLI bridge.
    result: Config Schema Design PR #457, Config Engine PR #460, and Config CLI Bridge PR #461 merged into migration/python-tui.
    changed:
      - Added root fortifylab.config schema metadata and parser/writer capability contract.
      - Added Python-native config engine with parser, validation, repair, backup, rollback marker, and redacted diffs.
      - Exposed validate, diagnostics, and repair-derived through ./bin/fortifylab config.
      - Preserved ./start_wizard.sh config-diagnostics as a compatibility alias.
    blockers: []
    risks:
      - Full-screen TUI config editor remains future work and should not be mixed into M5 diagnostics.
    last_heartbeat: 2026-08-25

  diagnostics:
    owner: diagnostics-agent
    status: complete
    branch: agent/test-M5-diagnostics
    owns:
      - fortifylab/status/
      - fortifylab/diagnostics/
      - tests/
    current_task: Completed read-only diagnostic/status contracts and doctor/status command behavior.
    result: Diagnostics/status PR #463 merged into migration/python-tui.
    changed:
      - Added root fortifylab.diagnostics and fortifylab.status contracts.
      - Added clone-safe doctor and status command behavior.
      - Added diagnostic redaction and injected-runner contracts.
      - Kept default tests free of cluster, network, Kubernetes, Helm, and Docker dependencies.
    blockers: []
    risks:
      - Diagnostics docs cleanup is deferred into M6 docs or M7 final docs sweep.
    last_heartbeat: 2026-08-25
  runbooks:
    owner: runbooks-agent
    status: complete
    branch: agent/runbooks-M6-implementation
    owns:
      - fortifylab/runbooks/
      - fortifylab/help.py
      - fortifylab/cli.py
      - tests/test_m6_runbooks_contract.py
      - tests/test_m6_runbooks_help.py
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Implement Python-native M6 runbook/help behavior from the merged contract and tests.
    changed:
      - Added runbook load, validation, preview, guarded run, command result, and confirmation APIs.
      - Added offline HelpRegistry topic lookup with alias preservation and typed lookup errors.
      - Added clone-safe CLI help topic check behavior.
      - Activated tests/test_m6_runbooks_help.py with no implementation-dependent skips.
    result: M6 runbook/help implementation PR #467 merged into migration/python-tui.
    next_task: Watch M7 audit for any retained runbook/help adapter boundaries.
    blockers: []
    risks:
      - Run execution must remain explicit and environment-dependent; default tests must not invoke lab scripts or live services.
    last_heartbeat: 2026-08-25
  docs:
    owner: docs-agent
    status: complete
    branch: agent/docs-M6-help-runbooks
    owns:
      - README.md
      - docs/
      - runbooks/README.md
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Completed implemented M6 help/runbook surface and M5 diagnostics command documentation.
    result: M6 docs PR #468 merged into migration/python-tui.
    changed:
      - Added Python preview examples for help, doctor, and status commands.
      - Documented runbook clone-safe preview/validation and environment-dependent execution boundaries.
      - Updated help mapping contribution guidance for the Python registry during migration.
    blockers: []
    last_heartbeat: 2026-08-25
  tests:
    owner: test-agent
    status: active
    branch: agent/test-M7-retirement
    owns:
      - tests/test_m1_entrypoints.py
      - tests/test_m2_menu_parity.py
      - tests/test_m4_config_engine.py
      - tests/test_m5_diagnostics.py
      - tests/test_m6_runbooks_help.py
      - tests/test_m7_retirement_contract.py
      - tests/quarantine/bash_wizard_internal/
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Added M7 retirement and compatibility regression tests without live cluster, network, Kubernetes, Helm, or Docker dependencies.
    result: In progress on agent/test-M7-retirement.
    next_task: Open M7 test PR and coordinate with cleanup/audit agents.
    blockers: []
    last_heartbeat: 2026-08-25
  retirement_audit:
    owner: retirement-audit-agent
    status: complete
    branch: agent/retirement-M7-audit
    owns:
      - .migration/python-tui-plan.yml
      - docs/development/python-tui-migration.md
    current_task: Audit M7 retirement targets and classify safe cleanup boundaries before deletion.
    changed:
      - Deprecated src preview package classified for removal after remaining reference-only tests/docs are migrated or retired.
      - tests/quarantine/python_preview classified as retire/remove after harvested concepts are represented by M1-M6 active tests.
      - tests/quarantine/bash_wizard_internal classified as quarantine/reference until compatibility entrypoints are locked, then remove or archive intentionally.
      - scripts/wizard/*.sh classified as migrate or remove, not low-level adapters, once Python guided/config/runbook/flight-plan parity is explicit.
      - M3 adapter-protected lifecycle scripts identified under apps/**/start.sh, stop.sh, and destroy.sh.
      - start_wizard.sh classified as keep as compatibility shim.
    next_task: Open audit PR, then hand off to compatibility/test, cleanup, and docs agents.
    blockers: []
    risks:
      - Do not delete app lifecycle scripts referenced by fortifylab.operations.catalog.
      - Preserve ./bin/fortifylab, ./start_wizard.sh, and ./start_wizard.sh config-diagnostics until compatibility tests say otherwise.
      - Update stale docs before users are told the Bash wizard remains the production guided app.
    last_heartbeat: 2026-08-25

milestones:
  - id: M0
    name: Migration branch and project controls
    status: complete
    acceptance:
      - migration/python-tui exists from current dev.
      - Branch and PR rules are documented.
      - Machine-readable tracker exists.
      - Human-readable migration document exists.
      - Heartbeat ritual is documented.
      - Initial risks and blockers are captured.
  - id: M1
    name: Python app skeleton and entrypoints
    status: complete
    acceptance:
      - Primary Python package skeleton exists.
      - ./bin/fortifylab --help works from a clone.
      - ./bin/fortifylab tui launches a placeholder TUI shell.
      - start_wizard.sh is either a deliberate shim or intentionally removed.
      - Entrypoint tests pass.
    completed_at: 2026-08-25
    pull_requests:
      - 451
      - 450
    evidence:
      - ./bin/fortifylab --help passed during Architecture review.
      - ./bin/fortifylab tui --smoke-test passed during Architecture review.
      - ./bin/fortifylab tui --check passed during Architecture and Test review.
      - python3 -m compileall -q fortifylab passed during Architecture and Test review.
      - python3 -m unittest tests.test_m1_entrypoints -v passed.
      - python3 -m unittest discover -s tests -v passed with 81 tests.
      - ./scripts/validate-docs.sh passed documentation quality gates in CI.
  - id: M2
    name: Navigation parity
    status: complete
    acceptance:
      - Python menu tree mirrors the current Bash wizard structure.
      - Arrow-key selection works.
      - Jump-to-number selection works.
      - Return, escape, help, and quit behavior are implemented.
      - Menu parity tests cover labels and ordering.
    completed_at: 2026-08-25
    pull_requests:
      - 454
      - 453
      - 455
    evidence:
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest tests.test_m2_menu_parity -v passed with 10 tests.
      - ./bin/fortifylab tui --check passed and prints the M2 menu.
      - python3 -m unittest discover -s tests -v passed with 99 tests.
      - PR #455 offline-docs CI passed before merge.
    decisions:
      - Number keys use jump-highlight behavior and require Enter or Return to activate.
      - Duplicate number keys are scoped to the current menu id.
      - Disabled menu items return disabled results with reasons instead of activating.
  - id: M3
    name: Operation adapters
    status: complete
    acceptance:
      - Existing Bash lifecycle scripts are represented in an operation catalog.
      - Mutating operations show dry-run previews and require confirmation.
      - Output streams into the TUI/log layer.
      - Exit codes and failures are surfaced clearly.
      - Secrets are redacted in logs.
    completed_at: 2026-08-25
    pull_requests:
      - 458
    evidence:
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest tests.test_m3_operation_adapters -v passed with 8 tests.
      - python3 -m unittest discover -s tests -v passed with 112 tests after config design merge.
      - PR #458 offline-docs CI passed before merge.
      - Tests do not use Kubernetes, Helm, Docker, network, or real lifecycle scripts.
    notes:
      - M3 returns captured command output; future TUI work may add incremental streaming.
      - Helmholtz branch agent/test-M3-operation-adapters was superseded by active tests in PR #458.
  - id: M4
    name: Python-native config editor
    status: complete
    acceptance:
      - .env parser preserves comments and ordering where practical.
      - Config updates create backups.
      - Diffs and secret redaction work.
      - Validation and derived URL repair are Python-native.
      - Bash config bridge is removed or no longer required.
    completed_at: 2026-08-25
    pull_requests:
      - 457
      - 460
      - 461
    evidence:
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest tests.test_m4_config_engine -v passed with 6 tests.
      - python3 -m unittest tests.test_m4_config_cli_bridge -v passed with 7 tests.
      - python3 -m unittest discover -s tests -v passed with 125 tests.
      - ./bin/fortifylab tui --check passed.
      - PR #461 offline-docs CI passed before merge.
    notes:
      - Python-native config behavior is local-file only in tests and reachable from supported CLI paths.
      - Full-screen TUI config editor remains future work.
  - id: M5
    name: Status and diagnostics
    status: complete
    acceptance:
      - Prerequisite, license, cluster, pod, registry, and TLS checks are available.
      - doctor command works without the Bash wizard.
      - Sanitized diagnostics bundle behavior is covered by tests.
    completed_at: 2026-08-25
    pull_requests:
      - 463
    merge_commit: 3964779fad3d13b5085c51f83c6b9fc3e0e4244f
    evidence:
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest tests.test_m5_diagnostics -v passed with 8 tests.
      - python3 -m unittest discover -s tests -v passed with 133 tests.
      - ./bin/fortifylab doctor --check passed.
      - ./bin/fortifylab status --check passed.
      - ./bin/fortifylab tui --check passed.
      - git diff --check passed.
    notes:
      - Doctor/status checks are clone-safe and read-only by default.
      - Live cluster, pod, registry, and TLS checks remain explicit environment-dependent behavior.
      - Diagnostics docs cleanup is deferred into M6 docs or the M7 final docs sweep.
  - id: M6
    name: Runbooks and help
    status: complete
    acceptance:
      - Runbook discovery and metadata validation are Python-native.
      - Help Center topics are reachable from the TUI.
      - Requirement checks and run previews work.
    completed_at: 2026-08-25
    pull_requests:
      - 466
      - 465
      - 467
      - 468
    merge_commits:
      - 5c4f136b38800d0a1886476cdd8c69ee3d5e28c0
      - 2281e6612c59ad92d1350044cd4266b0cc562341
      - 9580a17eb085ca7c7e245e12adb91d5c73b1c42c
      - 9ddef23249c9b3385967c8584e4fb0562c3cc4d5
    evidence:
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest tests.test_m6_runbooks_contract tests.test_m6_runbooks_help -v passed with 16 tests and no skips.
      - python3 -m unittest discover -s tests -v passed with 149 tests.
      - ./bin/fortifylab help topic guided/sast --check passed.
      - ./bin/fortifylab doctor --check passed.
      - ./bin/fortifylab status --check passed.
      - ./bin/fortifylab tui --check passed.
      - git diff --check passed.
      - GitHub offline-docs passed for M6 PRs.
    notes:
      - fortifylab.runbooks defines validation, script preview, and command preview as clone-safe.
      - fortifylab.runbooks defines run actions and live requirement checks as environment-dependent.
      - Navigation ids for Runbook Library, Help Center, and Operational guidance are preserved.
      - Local ./scripts/validate-docs.sh passed Markdown and unittest gates, then stopped because local mkdocs is not installed; remote offline-docs passed.
  - id: M7
    name: Bash wizard retirement
    status: complete
    completed_at: 2026-08-25
    merged_prs:
      - 470
      - 471
      - 472
      - 473
      - 474
      - 475
      - 476
    merge_commits:
      - ecd88585b2d0784f6a89032e9b1b13e138ea969a
      - 815acf74895ba8fc9c33002c9c9e38d8044caf08
      - 86c383d99531a0f9d2bb96efa07745ff3cd1dfc3
      - 0e09c0b3a307037a9fb69f468e4517e03a232e0a
      - 365f8ce6994b6fe3e459a0352d1d66617d03e95a
      - ea43c07c5026cdc5efe15837e39111ea157dce48
      - aca5774bb6a41588bdd76db013b132c89b167f04
    acceptance:
      - start_wizard.sh no longer contains the application wizard.
      - Deprecated src preview references are removed or intentionally migrated.
      - Docs and tests reflect the Python TUI as the supported path.
      - Bash remains only for low-level adapters that are still intentionally retained.
    evidence:
      - Final regression branch agent/test-M7-final-retirement-regression verified PR #475 base ea43c07.
      - Fixed stale scripts/tools/flight-plans.py help text that advertised retired start_wizard.sh apply-flight-plan examples as current workflow.
      - Retired paths remain absent: src/fortifylab, tests/quarantine/python_preview, and scripts/wizard.
      - Retired-reference scan found no active supported runtime/docs examples pointing users to deleted paths or retired commands; remaining hits are retirement guidance, contract tests, or quarantined historical Bash tests.
      - python3 -m compileall -q fortifylab passed.
      - python3 -m unittest discover -s tests passed with 165 tests.
      - ./bin/fortifylab doctor --check, status --check, tui --check, and config diagnostics passed.
      - ./start_wizard.sh --help, doctor, status, help topic guided/sast, and config-diagnostics passed.
      - ./scripts/validate-docs.sh passed Markdown and unittest gates, then stopped because local mkdocs is not installed.
      - ./scripts/tools/flight-plans.py --help passed and no longer advertises retired start_wizard.sh apply-flight-plan examples.
      - git diff --check passed.
    notes:
      - Audit branch agent/retirement-M7-audit found scripts/wizard internals are not M3 catalog dependencies and should not be kept as operation adapters.
      - Bash boundary branch agent/bash-M7-adapter-boundary removed scripts/wizard/*.sh after supported compatibility tests confirmed shim/config/doctor/status/help/tui paths no longer source them.
      - Source cleanup branch agent/src-M7-retirement removes src/fortifylab and tests/quarantine/python_preview after supported entrypoints resolve through the root fortifylab package.
      - M3 operation catalog keeps app lifecycle scripts as low-level adapters under apps/**/start.sh, stop.sh, and destroy.sh.
      - start_wizard.sh remains a minimal compatibility shim for M7 unless a focused compatibility PR retires it explicitly.
  - id: M8
    name: Merge back to dev
    status: held
    opened_at: 2026-08-25
    acceptance:
      - All milestone acceptance tests pass.
      - Migration tracker is complete.
      - Maintainer docs explain the final architecture.
      - migration/python-tui is ready for integration into dev.
    planned_agents:
      - branch: agent/release-M8-readiness
        scope: Audit tracker and docs consistency, verify M0-M7 complete, and prepare final merge-back checklist.
      - branch: agent/test-M8-final-verification
        scope: Rerun full tests, CLI compatibility checks, retired-path scans, adapter-reference checks, and docs gates.
      - branch: agent/merge-M8-dev-prep
        scope: Prepare PR summary from migration/python-tui to dev, collect milestone evidence, identify conflicts, and recommend merge strategy.
    notes:
      - M8 is governance and integration quality only; no new product features should be added.
      - M7 local docs validation passed Markdown and unittest gates but did not complete a full local docs site build because mkdocs is not installed locally.
      - PR #479 opened from migration/python-tui to dev and passed offline-docs, but user intentionally held the merge while M9 TUI workflow parity continues.
    merge_back_checklist:
      - Verify .migration/python-tui-plan.yml parses as YAML.
      - Confirm M0 through M7 are marked complete and M8 is marked in_progress.
      - Confirm README and development docs present ./bin/fortifylab as the primary Python CLI/TUI surface.
      - Confirm ./start_wizard.sh is documented only as an M7/M8 compatibility shim for supported aliases.
      - Confirm retained Bash scripts are described as low-level operation adapters, bootstrap helpers, or lifecycle scripts, not the interactive application.
      - Classify stale-reference scan hits as active guidance, compatibility shim, historical migration log, quarantine/reference material, or stale docs requiring cleanup.
      - Run full M8 verification on agent/test-M8-final-verification, including unit tests, CLI/shim smoke checks, retired-path scans, adapter-reference checks, docs validation, and git diff --check.
      - Prepare the migration/python-tui to dev PR with milestone evidence, known mkdocs local limitation, conflict strategy, and rollback guidance.
  - id: M9
    name: TUI submenu workflow parity
    status: in_progress
    opened_at: 2026-08-25
    branch: agent/pm-open-M9-tui-workflow-parity
    acceptance:
      - TUI submenu workflows are implemented one at a time until parity is met from the retired Bash wizard structure.
      - Top-level navigation organization remains stable.
      - Default tests stay clone-safe and non-mutating.
      - PR #479 remains held out of dev unless the user explicitly changes the release strategy.
    rules:
      - Do not redesign top-level navigation.
      - Do not implement more than one submenu/workflow per submilestone.
      - Reuse M4, M5, and M6 APIs instead of duplicating domain logic in Textual screens.
      - Do not require live Kubernetes, Helm, Docker, network, credentials, or lab state in default tests.
      - Do not delete retained Bash operation adapters.
      - Mutating operations must require confirmation and tests must use temp files, mocks, or harmless subprocesses only.
    submilestones:
      - id: M9.1
        name: Help/Runbooks workflow
        status: complete
        completed_at: 2026-08-26
        merged_prs:
          - 481
          - 483
          - 482
        merge_commits:
          - 67befecc58ddd2bbf725c200cc622293674c163e
          - 5058cb861b7a7adca55e579abf4c9639fb0ea10f
          - c390f46539f5ab2682a7cbc8f99a328478b1650e
        branches:
          - agent/tui-M9-workflow-dispatch-contract
          - agent/tui-M9-help-runbooks
          - agent/test-M9-help-runbooks-tui
        acceptance:
          - Help Center menu opens a functional help topic browser.
          - Runbook Library menu opens a functional runbook browser.
          - Users can list, select, view detail, preview, go back, and quit with existing key conventions.
          - Runbook execution remains preview-only or confirmation-gated.
          - Noninteractive tests verify workflow wiring without live lab dependencies.
        evidence:
          - Help Center TUI action opens a real offline topic browser with list/detail/back navigation.
          - Runbook Library TUI action opens discovered runbooks, metadata/detail, clone-safe command preview, and bounded script preview.
          - No unguarded runbook execution was added.
          - python3 -m compileall -q fortifylab passed.
          - python3 -m unittest discover -s tests passed with 175 tests.
          - ./bin/fortifylab tui --check, doctor --check, status --check, and help topic guided/sast --check passed.
          - git diff --check passed.
      - id: M9.2
        name: Config workflow
        status: complete
        completed_at: 2026-08-26
        merged_prs:
          - 486
          - 485
        merge_commits:
          - fc9b0b3d22e4248e177e0c8d7433c7c8c1aee80a
          - e28a4c1c35ac11865f6c74c944553832fe98826c
        branches:
          - agent/tui-M9.2-config-workflow
          - agent/test-M9.2-config-workflow
        acceptance:
          - Configuration menu opens a functional config workflow.
          - Diagnostics and validation render in the TUI.
          - Derived URL repair diff preview works.
          - Mutating repair requires confirmation.
          - Tests use temporary .env files only.
        evidence:
          - Configuration editor menu dispatch opens a real pure TUI workflow screen.
          - Diagnostics and validation render through existing M4 config CLI/engine APIs.
          - Derived repair preview renders redacted diffs without writing.
          - Repair writes are confirmation-gated and tested only against temporary .env fixtures.
          - python3 -m compileall -q fortifylab passed.
          - python3 -m unittest discover -s tests passed with 186 tests.
          - ./bin/fortifylab tui --check, doctor --check, status --check, and config diagnostics passed.
          - git diff --check passed.
      - id: M9.3
        name: Diagnostics and status workflow
        status: complete
        completed_at: 2026-08-26
        merged_prs:
          - 489
          - 488
        merge_commits:
          - 2dd7f4e30fa6702cb459a0909d1ae78964ef39cf
          - 7093a22d4f0c76c4cf971fdaf75dca2b16e8a4f9
        branches:
          - agent/tui-M9.3-diagnostics-status
          - agent/test-M9.3-diagnostics-status
        acceptance:
          - Doctor and status menu paths show real diagnostic result screens.
          - PASS, WARN, FAIL, and SKIP states render clearly.
          - Redaction applies to rendered output.
          - Live checks are mocked or safely skipped in default tests.
        evidence:
          - agent/tui-M9.3-diagnostics-status wires the Diagnostics menu to a combined Doctor/Status TUI screen.
          - Direct doctor and status workflow targets render focused clone-safe result screens through existing M5 APIs.
          - PASS, WARN, FAIL, and SKIP states are covered by pure TUI tests with injected fixtures.
          - Rendered detail and output pass through diagnostics redaction before display.
          - Refresh/back/quit actions are supported without Kubernetes, Helm, Docker, network, credentials, or live lab access.
          - python3 -m compileall -q fortifylab passed.
          - python3 -m unittest discover -s tests passed with 191 tests.
          - ./bin/fortifylab tui --check, doctor --check, and status --check passed.
          - git diff --check passed.
      - id: M9.4
        name: Lifecycle operation workflow
        status: complete
        completed_at: 2026-08-26
        merged_prs:
          - 491
          - 492
          - 494
        superseded_prs:
          - 493
        merge_commits:
          - 79503b900b6a53fec561c43b26bae7ba0cf00b65
          - 7ac6b7a2139a057f179b213a12907b359df0b5aa
          - 247a696ca3b8840bb350665712972deb3afbb52a
        branches:
          - agent/tui-M9.4-lifecycle-contract
          - agent/tui-M9.4-lifecycle-operations
          - agent/test-M9.4-lifecycle-operations
          - agent/docs-M9.4-lifecycle-workflow
        acceptance:
          - Lifecycle menu actions resolve to operation catalog entries.
          - Dry-run preview works before mutating execution.
          - Mutating execution requires confirmation.
          - Result and output displays are redacted.
          - Tests use mocked runners or harmless subprocess commands only.
        evidence:
          - agent/tui-M9.4-lifecycle-contract defines navigation-target to M3 operation catalog mappings for lab start/stop, component app lifecycle, and sample app lifecycle.
          - First supported operation set is start, stop, and destroy for MySQL, PostgreSQL, SSC, LIM, ScanCentral SAST, ScanCentral DAST, Juice Shop, WebGoat, and DVWA, plus all-lab start and stop.
          - Restart lab and reset lab remain unsupported placeholders until ordered sequence and destructive scope screens exist.
          - Dry-run preview, confirmation gating, result display, and output redaction contracts are covered by clone-safe TUI tests with injected runners only.
          - agent/tui-M9.4-lifecycle-operations wires app and sample app lifecycle screens to the confirmed operation runner, keeps preview/confirmation gates, renders redacted result output, and adds mocked runner workflow tests.
          - Active workflow screens receive printable action keys in Textual mode, while lifecycle operation selection supports both arrow keys and number keys.
          - agent/docs-M9.4-lifecycle-workflow documents supported lifecycle targets, confirmation flow, redacted/bounded output, clone-safe test boundaries, and restart/reset limitations.
          - python3 -m compileall -q fortifylab passed.
          - python3 -m unittest discover -s tests -v passed with 213 tests.
          - ./bin/fortifylab tui --check, doctor --check, and status --check passed.
          - git diff --check passed.
      - id: M9.5
        name: Logs and wizard log workflow
        status: complete
        completed_at: 2026-08-26
        merged_prs:
          - 496
          - 497
        merge_commits:
          - 0d9218232a778ad7e9ed8748df81ba4eae45cc6a
          - 67752f0a858a04ee6620d5404a8384fbded9e69a
        branches:
          - agent/pm-open-M9.5-logs-workflow
          - agent/test-M9.5-logs-workflow
          - agent/close-M9.5-logs-workflow
        acceptance:
          - Logs menu actions open functional TUI workflow screens.
          - Wizard log view opens a functional log view or clear unavailable state.
          - Available logs can be listed, viewed, refreshed, and exited without mutation.
          - Log content is redacted before display.
          - Missing logs do not crash the TUI.
          - Tests use temp fixture logs only and require no live lab state.
        evidence:
          - M9.5 opened after PM recommendation as the read-only workflow bridge after lifecycle operations.
          - fortifylab.logging now defines read-only log source discovery, wizard log path resolution, bounded tail/detail reads, missing/unavailable/unsafe states, and log redaction.
          - Logs and Wizard Log menu actions dispatch to TUI workflow screens with arrow/number source selection, refresh, and back behavior.
          - Wizard log discovery mirrors the legacy XDG state layout without creating files or directories.
          - Log workflow tests use temporary fixture logs only and do not require Kubernetes, Helm, Docker, network, credentials, or live lab state.
          - python3 -m compileall -q fortifylab passed.
          - python3 -m unittest discover -s tests -q passed with 229 tests.
          - ./bin/fortifylab tui --check, doctor --check, and status --check passed.
          - git diff --check passed.
      - id: M9.6
        name: Setup and readiness workflow
        status: completed
        branches:
          - agent/pm-open-M9.6-readiness-workflow
          - agent/readiness-M9.6-contract
          - agent/tui-M9.6-readiness-workflow
          - agent/test-M9.6-readiness-workflow
          - agent/docs-M9.6-readiness-workflow
        acceptance:
          - Initial setup/readiness opens a functional TUI workflow screen.
          - Readiness overview aggregates existing diagnostics/status/config signals without duplicating domain logic.
          - Config, license, prerequisite, and live-lab unavailable states are visible and redacted.
          - Recommended actions hand off to existing Config, Help, Doctor/Status, Lifecycle, or Logs workflows.
          - Refresh/back/quit behavior works.
          - Tests use fixtures/mocks only and require no live lab state.
        evidence:
          - M9.6 opened after PM recommendation as the safer start-here workflow before full guided deployment.
          - PR #501 defined the setup readiness snapshot, signal states, recommended workflow handoffs, refresh/back contract, and clone-safe provider injection boundary.
          - PR #502 wired setup_readiness, setup_readiness.guided_steps, and setup_readiness.reset_tiers to functional Python TUI screens.
          - The Textual app routes readiness recommended-action handoffs into existing Config, Doctor, Status, Help, Lifecycle, and Logs workflows through WorkflowKeyResult.open_target.
          - Reset tiers remain read-only and hand off to existing lifecycle/logs/help workflows without running destructive operations directly.
          - python3 -m compileall -q fortifylab tests passed.
          - python3 -m unittest discover tests passed with 241 tests.
          - ./bin/fortifylab tui --check, doctor --check, and status --check passed.
          - git diff --check passed.
      - id: M9.7
        name: Guided deployment workflow
        status: completed
        branches:
          - agent/pm-open-M9.7-guided-deployment
          - agent/deploy-M9.7-contract
          - agent/test-M9.7-guided-deployment
          - agent/tui-M9.7-guided-deployment
          - agent/docs-M9.7-guided-deployment
        acceptance:
          - Deploy menu opens a functional guided deployment workflow.
          - Profile selection works and is represented as explicit workflow state.
          - Deployment mode selection works and preserves the existing navigation intent.
          - Guided steps are represented as explicit state with progress/result rendering.
          - Mutating steps require dry-run/preview and explicit confirmation before execution.
          - Cancelled steps do not execute operations.
          - Resume/repair paths are represented or clearly unavailable.
          - Completion state offers handoffs to Diagnostics/Status, Logs, Help, and Lifecycle.
          - Tests use injected fake runners only and require no live lab state.
        evidence:
          - M9.7 opened after M9.6 closeout as the next focused parity gap before any dev merge.
          - PR #479 remains held out of dev unless the user explicitly changes the release strategy.
          - PR #504 opened the guided deployment workflow milestone and guardrails.
          - PR #506 defined the guided deployment state machine, profile/mode/step contracts, preview/confirmation/cancel behavior, fake-runner boundary, completion handoffs, and workflow registration.
          - PR #505 added active workflow coverage for preserved Deploy navigation, selection, preview, confirmation, cancellation, redaction, unavailable resume/repair states, and completion handoffs.
          - python3 -m compileall -q fortifylab tests passed.
          - python3 -m unittest discover tests passed with 257 tests.
          - ./bin/fortifylab tui --check, doctor --check, and status --check passed.
          - git diff --check passed.
          - GitHub offline-docs passed on PRs #504, #505, and #506.
      - id: M9.8
        name: Dashboard access, URLs, and credentials workflow
        status: proposed
        branches:
          - agent/pm-open-M9.8-dashboard-urls-credentials
          - agent/access-M9.8-contract
          - agent/test-M9.8-dashboard-urls-credentials
          - agent/tui-M9.8-dashboard-urls-credentials
          - agent/docs-M9.8-dashboard-urls-credentials
        acceptance:
          - Dashboard access and URLs/credentials menu paths open functional TUI workflow screens.
          - Values come from existing config/status helpers where possible.
          - Secrets are masked by default, with explicit reveal only if a later approved implementation adds it.
          - Missing values produce clear safe states and handoffs to Config, Diagnostics/Status, and Help.
          - Tests use fixture config/state only and require no live lab state.
        evidence:
          - PM recommended M9.8 after M9.7 because users need safe post-deployment access surfaces after guided deployment.
          - Awaiting user approval before activation.
    verification_gates:
      - python3 -m compileall -q fortifylab
      - python3 -m unittest discover -s tests -v
      - ./bin/fortifylab tui --check
      - ./bin/fortifylab doctor --check
      - ./bin/fortifylab status --check
      - git diff --check
