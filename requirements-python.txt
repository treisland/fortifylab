# Fortify Lab Python runtime dependencies for Phase 3 CLI/TUI migration.
# The clone-and-run preview commands still work from source with the Python
# standard library. Install this file when developing or previewing richer
# Python CLI/TUI workflows.
typer>=0.12,<1
rich>=13.7,<14
pydantic>=2.7,<3
textual>=0.70,<1
