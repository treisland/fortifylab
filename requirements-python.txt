# FortifyLab Python runtime dependencies for the CLI/TUI migration.
#
# M1 uses only the Python standard library so ./bin/fortifylab --help and
# ./bin/fortifylab tui --smoke-test work from a clean clone. Textual is the
# accepted framework for the interactive TUI that starts in M2.
typer>=0.12,<1
rich>=13.7,<14
pydantic>=2.7,<3
textual>=0.70,<1
